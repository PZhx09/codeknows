<head>YEP!!!!</head>

<table>

 
  <% @articles.each do |article| %>
    <tr>
    <td><%= link_to article.title, article_path(article) %></td>
      <td><%= article.body %></td>
    </tr>
  <% end %>
</table>